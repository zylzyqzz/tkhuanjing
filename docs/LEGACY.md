# 旧版兼容代码

根目录的 `backend.py`、`client_src/`、`client_launcher.py`、旧版 spec 与旧版安装脚本仅用于数据格式和旧客户端接口对照。V2 的正式开发入口为 `server/`、`client_v2/`、`admin/`、`scripts/build.ps1` 和 `installer_v2_product.iss`，旧文件不再继续补丁式开发。
