# 历史数据兼容

V2.10 已删除旧 Tk 客户端、旧单文件服务端、旧构建规格和会覆盖历史版本的部署脚本。正式入口仅为 `server/`、`client_v2/`、`admin/`、`scripts/build.ps1` 和 `installer_v2_product.iss`。

报告 Schema 4/5 仍可只读打开；其中的直播软件、设备和客户端分类不参与 V2.10 结论、标签或统计。新报告 Schema 6 只接受网络、系统和性能三类结果。
